# SHADOW // SYSTEM — V2

Replace the existing `index.html`, `style.css`, and `script.js` in your GitHub repository with these files.

GitHub Pages will rebuild after the commit.

Features:
- Boot sequence
- Interactive cursor glow
- Responsive layout
- Project modules
- Timeline
- Interactive terminal
- GitHub profile link
- No framework or build step
